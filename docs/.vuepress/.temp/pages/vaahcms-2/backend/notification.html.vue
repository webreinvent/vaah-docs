<template><div><h1 id="notification" tabindex="-1"><a class="header-anchor" href="#notification" aria-hidden="true">#</a> Notification</h1>
<h2 id="introduction" tabindex="-1"><a class="header-anchor" href="#introduction" aria-hidden="true">#</a> Introduction</h2>
<p>Through this panel you can control all the notification related things like what subject, body, from etc.</p>
<p>Visit following url you will see the Notification section:</p>
<div class="language-http line-numbers-mode" data-ext="http"><pre v-pre class="language-http"><code>&lt;public-url>/backend#/vaah/settings/notifications
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Below Image is an example of notification.</p>
<img :src="$withBase('/images/notification-setting-1.png')">
<h2 id="usage" tabindex="-1"><a class="header-anchor" href="#usage" aria-hidden="true">#</a> Usage</h2>
<h4 id="add-into-notifications" tabindex="-1"><a class="header-anchor" href="#add-into-notifications" aria-hidden="true">#</a> Add into notifications</h4>
<p>For adding need to click <code v-pre>Add</code> button, at the top-right corner.
like below image :</p>
<img :src="$withBase('/images/notification-setting-2.png')">
<p>After clicking <code v-pre>Add</code> a text field section will expand below message with <code v-pre>save</code> button.
Fill the field by notification name and click on <code v-pre>save</code> it will appear in the list below.</p>
<img :src="$withBase('/images/notification-setting-3.png')">
<h4 id="filter-of-notification" tabindex="-1"><a class="header-anchor" href="#filter-of-notification" aria-hidden="true">#</a> Filter of notification</h4>
<p>A search field is there which will filter according to notification title, by using filter one can edit notifications.</p>
<img :src="$withBase('/images/notification-setting-4.png')">
<p>Below video for better understanding.</p>
<h4 id="how-to-edit-notification" tabindex="-1"><a class="header-anchor" href="#how-to-edit-notification" aria-hidden="true">#</a> How to edit notification</h4>
<p>To edit notification you need to click the edit button present on the right side of the list.</p>
<img :src="$withBase('/images/notification-setting-5.png')">
<p>After clicking this button edit page will open like below image :</p>
<img :src="$withBase('/images/notification-setting-6.png')">
<p><code v-pre>Go Back</code> button will redirect to notification list page</p>
<p>There is various options to send notification like below image:</p>
<img :src="$withBase('/images/notification-setting-7.png')">
<p>One can use those options by enabling toggles. Once toggle is enabled then respective tab will appear below.</p>
<p>Like below image :</p>
<img :src="$withBase('/images/notification-setting-8.png')">
<p>Once Tab is visible then click on tab and a button <code v-pre>Add content</code> will appear. By clicking it will add fields for content.</p>
<img :src="$withBase('/images/notification-setting-9.png')">
<img :src="$withBase('/images/notification-setting-10.png')">
<p>After put content according to requirement one can save it by clicking <code v-pre>save</code> button present in the bottom-left corner
Or test by clicking <code v-pre>Test</code> button besides it.</p>
<img :src="$withBase('/images/notification-setting-11.png')">
<p>A text field will be available in a extended section under Test button by clicking <code v-pre>Test</code> button.
One can type anything it will search users and by selecting user notification can be tested clicking <code v-pre>Send</code> button.</p>
<img :src="$withBase('/images/notification-setting-12.png')">
<p>Follow below videos for better understanding:</p>
<figure>
  <iframe src="https://img-v4.getdemo.dev/screenshot/chrome_UGS71dEP36.mp4" frameborder="0" allowfullscreen="true" style="width: 100%; aspect-ratio: 16/9;"> </iframe>
</figure></div></template>


