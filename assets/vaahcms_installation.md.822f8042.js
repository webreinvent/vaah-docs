import{o as e,c as a,d as o}from"./app.307b76b9.js";const l='{"title":"Installation","description":"","frontmatter":{"title":"Installation","editLink":true},"headers":[{"level":2,"title":"Install via VaahCLI","slug":"install-via-vaahcli"},{"level":2,"title":"Manual Download","slug":"manual-download"},{"level":2,"title":"Install Dependencies","slug":"install-dependencies"},{"level":2,"title":"Setup & Configuration","slug":"setup-configuration"}],"relativePath":"vaahcms/installation.md","lastUpdated":1626351625193}',n={},t=o('<h1 id="installation"><a class="header-anchor" href="#installation" aria-hidden="true">#</a> Installation</h1><h2 id="install-via-vaahcli"><a class="header-anchor" href="#install-via-vaahcli" aria-hidden="true">#</a> Install via VaahCLI</h2><p>To install VaahCMS via VaahCLI, first you need to install VaahCLI, via following command:</p><div class="language-sh"><pre><code>npm install vaah -g\n</code></pre></div><p>Then run following command to install VaahCMS:</p><div class="language-sh"><pre><code>vaah cms:install\n</code></pre></div><h2 id="manual-download"><a class="header-anchor" href="#manual-download" aria-hidden="true">#</a> Manual Download</h2><p>You can manually download the setup files, from following link:</p><p><a href="https://github.com/webreinvent/vaahcms-ready/archive/master.zip" target="_blank" rel="noopener noreferrer">Download VaahCMS</a></p><h2 id="install-dependencies"><a class="header-anchor" href="#install-dependencies" aria-hidden="true">#</a> Install Dependencies</h2><p>VaahCMS utilizes <a href="https://getcomposer.org/" target="_blank" rel="noopener noreferrer">Composer</a> to manage its dependencies. So, before using Laravel, make sure you have Composer installed on your machine.</p><ol><li><p>Extract the downloaded VaahCMS folder.</p></li><li><p>Open terminal or command prompt and open the root folder of VaahCMS files.</p></li><li><p>Run following command to install the dependencies:</p><div class="language-sh"><pre><code>composer install\n</code></pre></div></li></ol><h2 id="setup-configuration"><a class="header-anchor" href="#setup-configuration" aria-hidden="true">#</a> Setup &amp; Configuration</h2><p>Once the dependencies are downloaded, you need to publish the assets of VaahCMS via following command:</p><div class="language-sh"><pre><code>php artisan vendor:publish --provider=&#39;WebReinvent\\VaahCms\\VaahCmsServiceProvider&#39; --tag=assets --force\n</code></pre></div><p>or</p><p>If you get this error: <code>Unable to locate publishable resources</code>.</p><p>Then just run publish command and choose the class that you want to publish.</p><div class="language-sh"><pre><code>php artisan vendor:publish\n</code></pre></div><p>If you&#39;re using XAMPP or WAMP, visit following url to begin the setup:</p><div class="language-sh"><pre><code>&lt;project-url&gt;/vaahcms/setup\n</code></pre></div><p>or</p><p>If you have PHP installed locally and you would like to use PHP&#39;s built-in development server to serve your application, you may use the serve Artisan command. This command will start a development server at <code>http://localhost:8000</code>. Run following command to start the serving the application:</p><div class="language-sh"><pre><code>php artisan serve\n</code></pre></div><p>and visit:</p><div class="language-"><pre><code>http://localhost:8000/vaahcms/setup\n</code></pre></div><p>This is will begin setup and easy form wizard to configure your the application. Once you&#39;re done with the configuration you will be able to login to the admin control panel at following url:</p><p>In case of XAMPP or WAMP:</p><div class="language-"><pre><code>&lt;project-url&gt;/backend\n</code></pre></div><p>In case of development server:</p><div class="language-"><pre><code>http://localhost:8000/backend\n</code></pre></div>',31);n.render=function(o,l,n,i,s,r){return e(),a("div",null,[t])};export default n;export{l as __pageData};
