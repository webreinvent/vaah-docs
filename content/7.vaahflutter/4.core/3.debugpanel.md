---
title: Debug panel
---

::alert{type="warning" class="p-4 mb-4 text-sm text-yellow-800 rounded-lg bg-yellow-50 dark:bg-gray-800 dark:text-yellow-300" role="alert"}

Dependencies

- This panel uses EnvironmentConfig, thus depending on the env.dart file.
::

<img width="512" src="/images/flutter/debug-panel.gif" alt="debug-panel">

Debug panel will be visible only if `showDebugPanel` is set to `true` for `EnvironmentConfig` in the `env.dart` file. You can change the color of the tag by setting the `debugPanelColor` variable for your EnvironmentConfig.

::alert{type="danger" class="flex items-center p-4 mb-4 text-sm text-red-800 rounded-lg bg-red-50 dark:bg-gray-800 dark:text-red-400" role="alert"}
Note:   
Remember showDebugPanel for production should always be `false` in the Environment configuration in the `env.dart` file.
::

```dart
  'production': defaultConfig.copyWith(
    ...
    showDebugPanel: false,
  ),
```

## Make the panel visible on a specific screen

```dart
final _navigatorKey = GlobalKey<NavigatorState>();

class MyPage extends StatelessWidget {
  const MyPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return DebugWidget(
        navigatorKey: _navigatorKey,
        child: Container(),
    );
  }
}
```

## Make the panel visible on every screen using central implementation.

::alert{type="info" class="flex items-center p-4 mb-4 text-sm text-blue-800 rounded-lg bg-blue-50 dark:bg-gray-800 dark:text-blue-400" role="alert"}
NOTE:   
You have to write the below code in MaterialApp, and that will show the tag panel on each screen. You don't have to wrap any other screen/ widget, or you don't have to extend any screen/widget with DebugWidget.
::

In the file containing the material app paste this code after imports
```dart
final _navigatorKey = GlobalKey<NavigatorState>();
```

 In the material app paste, this code and panel will be visible on all pages.
```dart
return MaterialApp(
  builder: (BuildContext context, Widget? child) {
    return DebugWidget(
      navigatorKey: _navigatorKey,
      child: child!,
    );
  },
);
```

## Adding a Custom Section to the Debug Panel

### About Custom Section

A Custom Section is a widget responsible for displaying custom data when a developer wants to showcase information not natively included in the debug panel or environment configuration.

### Components of the Custom Section Widget

Before adding a custom section, it's important to understand three main components:

1. **custom_debug_sections** (Type: Array of Objects)
   - **Description**: This is the main array that contains all the custom sections to be added to the debug panel.

2. **Array Elements**
   - Each element in the `custom_debug_sections` array represents a section in the debug panel.

3. **Section Object**
   - **section_name** (Type: String)
     - **Description**: The name of the section to be displayed in the debug panel.
   - **content_holder** (Type: Object)
     - **Description**: Holds the content for the section in a structured format.
   - **content** (Type: Object)
     - **Description**: Contains the key-value pairs that will be displayed as a table within the section.
     - **Key** (e.g., "Test Topic 1")
       - **Type**: String
       - **Description**: Represents the topic or the row heading in the table.
     - **Value**
       - **Type**: Object
       - **Description**: The value object holds the actual content for the topic.
       - **value** (Type: String)
         - **Description**: The value corresponding to the topic which will be displayed in the table.
       - **color** (Type: int, optional)
         - **Description**: The color code for the value. If not provided, the default color will be `Warning`.
         - **Example Colors**:
           - **Success**: 4283413805
           - **Warning**: 4294950656
           - **Danger**: 4294916141
           - **Info**: 4283214079
           - **White**: 4294967295
           - **Black**: 4278190080

### Example JSON Configuration

To display custom data on the debug panel, add the data to the relevant JSON configuration file (`develop.json`, `staging.json`, `production.json`, etc.).

Add a key to the JSON file, `custom_debug_sections`, with a list of objects containing section information as shown below:

```json
{
  // Other Content

  "custom_debug_sections": [
    {
      "section_name": "Test Section 1",
      "content_holder": {
        "content": {
          "Test Topic 1": {
            "value": "Test Value 1",
            "color": 4283413805
          }
        }
      }
    },
    {
      "section_name": "Test Section 2",
      "content_holder": {
        "content": {
          "Test Topic 1": {
            "value": "Test Value 1",
            "color": 4294916141
          },
          "Test Topic 2": {
            "value": "Test Value 2",
            "color": 4283214079
          }
        }
      }
    },
    {
      "section_name": "Test Section 3",
      "content_holder": {
        "content": {
          "Test Topic 1": {
            "value": "Test Value 3",
            "color": 4294967295
          }
        }
      }
    }
  ]
}

```

### Output from the above snippet

<img width="384" src="/images/flutter/custom-debug-panel.png" alt="custom-debug-panel">

### Additional Information
For more information about environment configuration, refer to the relevant documentation [here](../3.essentials/2.environments.md).

### Summary
- Understand the Components: Ensure you understand the structure and purpose of `custom_debug_sections`, section objects, and their attributes.
- Edit the JSON Configuration: Add the `custom_debug_sections` key with the required sections and their content to your environment configuration JSON file.